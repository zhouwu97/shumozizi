# 沈阳理工大学校外访问记录

## 已观察链路

```text
中国知网首页
→ https://fsso.cnki.net/
→ 沈阳理工大学统一认证 idp.sylu.edu.cn
→ 返回中国知网
```

页面显示 CARSI 标识，当前记录为 `carsi-saml`；这是访问路由记录，不是凭据记录。

## 后续操作规则

- 已认证会话仍有效时，AI 可以直接在知网中按检索计划操作，记录候选标题、作者、年份、摘要核验级别和来源 URL。
- 不重新打开登录页，不读取浏览器密码管理器，不导出 Cookie，不把会话信息写入运行目录。
- 跳回 `idp.sylu.edu.cn`、出现验证码或访问权限提示时停止，交还用户完成认证或授权。
- 只处理用户明确选择的少量候选；不批量翻页、并发抓取或批量下载全文。
