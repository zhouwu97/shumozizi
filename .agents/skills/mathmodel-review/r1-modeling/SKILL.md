---
name: mathmodel-review-r1-modeling
description: 独立建模审核，检查题意、变量、目标、约束、算法和验证计划。
---

# R1 建模审核

只读取题面、附件摘要、路线锁、模型规格和验证计划，输出 `review_report.json` 与回执。结论只能是 `ACCEPT`、`ACCEPT_WITH_FIXES` 或 `REBUILD`。审核任务不得修改作者代码、结果和论文。
