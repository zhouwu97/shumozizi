#set document(title: "母亲身心指标与婴儿睡眠及行为的统计关联")
#set page(margin: (x: 2cm, y: 1.7cm))
#set text(size: 9.2pt)
#set heading(numbering: "1.1")

= 母亲身心指标与婴儿睡眠及行为的统计关联

== 摘要

针对 390 组母婴横截面观测，本文共享清洗样本、基础协变量字典和固定验证折，但为睡眠时长、
睡醒次数、行为特征与入睡方式分别构造排除全部响应字段的特征矩阵。五折结果显示，睡眠时长
OLS 的 RMSE 为 1.4576 h，高于同折均值基线 1.4507 h，$R^2=-0.0167$；睡醒次数 Poisson
回归 MAE 为 1.2172 次，较基线 1.2333 次仅下降 1.3%，且 Pearson 离散度为 1.7322。行为
准确率 0.5769 与多数类基线相同；入睡方式准确率 0.4333 仅略高于 0.4282 的基线。结果没有
形成稳定、实质的样本外增益，只支持“当前基础协变量不足以稳定预测四类婴儿结果”的边界结论。

== 问题分析与证据边界

题目包含连续、计数和多类别响应，不能用同一种损失统一处理。广义线性模型为这些响应提供了
一致但分布匹配的框架#super("[1]")；计数模型还必须检验均值—方差假设和过度离散#super("[2]")。
母亲产后情绪与婴儿睡眠在既有研究中存在关联#super("[3]")，但本题为横截面观察数据，不能据此
建立方向性因果链。本文的回答顺序是：先与无协变量基线公平比较，再检查样本外误差与诊断，
最后决定哪些结论应被否定或降级。

== 数据、变量与防泄漏设计

保留前 390 组完整观测；一个异常睡眠时间按预注册规则做中位数填补。基础数值协变量为母亲
年龄、妊娠周数、CBTS、EPDS、HADS 和婴儿月龄；婚姻、教育、分娩方式和婴儿性别作哑变量
编码。四个响应为 $Y_s,Y_w,Y_b,Y_m$，对应特征矩阵为 $X_s,X_w,X_b,X_m$。

记不含任何响应的基础协变量集合为 $C$。四个矩阵都由 $C$ 编码得到，但各自独立拟合预处理器：

$ X_r := "Encode"_r(C), quad r in {s,w,b,m}. $

原始睡眠时间和派生小时数同时从 $X_s$ 排除，当前目标的任何 one-hot 派生列也被禁止。该约束
避免把目标自身编码成输入；分类内部验证使用分层五折，连续与计数模型使用同一随机种子的普通
五折。内部验证用于估计泛化误差，而不是证明外部可迁移性#super("[4]")。

== 基线、模型与指标

连续响应使用 $hat(y)=X_s beta$；计数响应满足
$log mu_w=X_w beta$；类别 $k$ 的多项 Logistic 概率为

$
  P(Y_r=k; X_r)=exp(x beta_k) / sum_j exp(x beta_j),
  quad r in {b,m}.
$

#table(
  columns: (1.05fr, 1.35fr, 1.55fr, 1.65fr),
  align: (left, left, left, left),
  [响应], [同折基线], [主模型], [样本外判据],
  [睡眠时长], [训练折均值], [OLS], [RMSE、$R^2$],
  [睡醒次数], [训练折均值], [Poisson], [MAE、离散度],
  [行为特征], [训练折多数类], [多项 Logistic], [准确率、宏 F1],
  [入睡方式], [训练折多数类], [多项 Logistic], [准确率、宏 F1],
)

所有 baseline 只从训练折估计，与主模型共享同一测试折。该约束比“全样本均值对样本外模型”
更严格，也避免基线看到测试折标签。

== 结果与逐项回答

#table(
  columns: (1.2fr, 1.25fr, 1.25fr, 1.2fr, 2fr),
  align: (left, right, right, right, left),
  [响应], [基线], [主模型], [辅助指标], [直接回答],
  [睡眠时长], [1.4507 h], [1.4576 h], [$R^2=-0.0167$], [未超过均值基线],
  [睡醒次数], [1.2333 次], [1.2172 次], [离散度 1.7322], [改善小且方差假设存疑],
  [行为特征], [0.5769], [0.5769], [宏 F1 0.3506], [没有分类增益],
  [入睡方式], [0.4282], [0.4333], [宏 F1 0.2298], [仅 0.5 个百分点增益],
)

睡眠时长的负 $R^2$ 表明，当前 OLS 在样本外还不如训练折均值。睡醒次数的 MAE 只下降
0.0161 次，而离散度显著大于 1，故 Poisson 的等离散假设不足；在完成负二项对照前不能提高
结论强度。行为模型完全没有超过多数类，入睡方式的微小差异也不足以抵消较低宏 F1。

#figure(
  image("../../../runs/desktop-e2e-20260719-training/figures/out_of_sample_diagnostics_v5.png", width: 96%),
  caption: [睡眠时长五折样本外预测及两个分类响应的混淆矩阵。对角线稀疏说明准确率不能单独代表各类别识别能力。]
)

#figure(
  image("../../../runs/desktop-e2e-20260719-training/figures/classification_cv_scores_v4.png", width: 72%),
  caption: [行为和入睡方式逐折准确率。折间波动进一步限制了稳定预测主张。]
)

== 局限、模型评价与推广

本稿的优势是响应字段隔离、同折基线和负结果可报告；不足包括单次五折、缺少外部样本、未给出
系数区间、未比较负二项模型，也没有处理潜在的家庭环境混杂。结果只能用于提出后续纵向研究
假设，不适合临床筛查、个体决策或因果解释。若扩展，应优先预注册外部验证与负二项对照，而
不是继续堆叠复杂分类器。

== 结论

在消除目标泄漏并统一样本外口径后，四类模型都没有形成稳定、实质的 baseline 增益。最直接
的题目回答不是“发现了高精度关系”，而是当前基础母婴协变量不足以稳定预测婴儿睡眠、睡醒、
行为和入睡方式；任何更强结论都需要纵向数据、外部验证和更合适的计数方差模型。

== 参考文献

#set enum(numbering: "[1]")
#enum(
  [McCullagh P, Nelder JA. _Generalized Linear Models_. Routledge, 2019. DOI: 10.1201/9780203753736.],
  [Cameron AC, Trivedi PK. _Regression Analysis of Count Data_. Cambridge University Press, 1998. DOI: 10.1017/CBO9780511814365.],
  [Skodova Z, Kelcikova S, Maskalova E, Mazuchova L. Infant sleep and temperament characteristics in association with maternal postpartum depression. _Midwifery_, 2022, 105: 103232. DOI: 10.1016/j.midw.2021.103232.],
  [Steyerberg EW. _Clinical Prediction Models_. Springer, 2019. DOI: 10.1007/978-3-030-16399-0.],
)
