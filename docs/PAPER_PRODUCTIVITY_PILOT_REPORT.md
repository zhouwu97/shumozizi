# 论文生产力优先修复试点交付报告

日期：2026-07-20  
分支：`pilot/paper-productivity`  
基线：`b5559c8d9dbddea99d9dc335de616a044193b1d7`

## 1. 结论

本轮已完整执行评审提出的四项整改：核查并修复入睡方式目标泄漏、完成三名独立评阅者的匿名
盲评、补充光学参数反演知识并改造双轴检索、建立一套完整多问旧题验证运行。

阶段结论如下：

- 目标泄漏已确认、修复并复现；旧结果已撤销论文资格，新结果封条有效。
- 路线 Gate B 和论文 Gate C 均通过，且三名评阅者一致选择整改后的版本。
- 双轴检索已阻止“通用结构原则”被误报为领域高相关，并在光学旧题上得到真实高置信匹配。
- 完整旧题已完成题面、附件、检索、三路线竞争和确认请求，但严格停在人工路线确认点，尚未
  启动正式实验或论文写作。
- 现有证据证明论文工作台显著改善同一实验事实的组织、验证口径和结论边界；它仍不证明知识库
  提高了模型样本外性能，也不构成稳定竞赛获奖能力证明。

因此，本试点可由“有条件通过”提升为“整改项通过、完整赛题验证待人工继续”，但仍不应作为
稳定生产能力直接合并或对外作过度声明。

## 2. 目标泄漏修复

实际代码审计确认旧 `q1-primary` 的入睡方式分类矩阵包含目标派生列，训练准确率 1.0000 属于
目标泄漏。该结果已标记为 `superseded` 且 `paper_allowed=false`，不得再进入论文。

修复后四类响应采用相同清洗样本和响应特异的特征矩阵：

| 响应 | 目标字段 | 输入原则 | 强制排除字段 | 结论 |
| --- | --- | --- | --- | --- |
| 睡眠时长 | `sleep_hours` | 基础协变量 | 全部响应字段及 `sleep_hours` 派生列 | 无泄漏 |
| 夜间睡醒次数 | `wake_count` | 基础协变量 | 全部响应字段及 `sleep_hours` | 无泄漏 |
| 婴儿行为特征 | `behavior` | 基础协变量 | 全部响应字段及 `sleep_hours` | 无泄漏 |
| 入睡方式 | `sleep_method` | 基础协变量 | `sleep_method` 全部派生列、其他响应字段及 `sleep_hours` | 无泄漏 |

新准入结果为：

- `q1-primary-leakage-fixed-v4`，执行记录 `q1-mixed-models-leakage-fixed-v4`；
- `q1-robustness-cv-baselines-v1`，执行记录 `q1-cv-baselines-v1`。

两项均为 `accepted`、`paper_allowed=true`。`verify_sealed_result()` 对两项均返回
`valid=true` 且无错误。

独立 R2 在两个隔离目录中执行，退出码均为 0；核心 JSON 逐字节一致，15 个指标的两次执行
差值均为 0。结论为 `REPRODUCIBLE_WITH_WARNINGS`，没有阻断 finding。R2 报告 SHA-256 为
`a518d06f9f7a594729f349d7a1b93065ca7e7fee0cbec499403b2e745b42456e`。

保留四项非阻断 P2：执行清单序列化存在差异；一个睡眠目标在交叉验证前使用全样本中位数填补；
primary 拟合预算少报；新旧结果的双向 supersede 指针语义需要后续收敛。这些问题不改变本次
复现结论，但应在稳定化前修复。

## 3. 独立匿名盲评

三名评阅者分别从统计建模、论文逻辑和自然竞赛评委视角独立评阅。输入使用哈希冻结的匿名
编号，评阅时未披露 A/B 映射、历史分数或本报告内容；三份报告提交后才汇总。

### 3.1 Gate B：路线

| 评阅者 | R17 | R42 | 选择 |
| --- | ---: | ---: | --- |
| 统计建模 | 64 | 96 | R42 |
| 论文逻辑 | 66 | 88 | R42 |
| 竞赛评委 | 61 | 92 | R42 |
| 平均 | 63.6667 | 92.0000 | 差值 28.3333 |

3/3 选择 R42，R42 的数学正确性和数据可行性均未下降，无 P0；因此
`gate_b_pass=true`。旧的单评阅 `93 vs 72` 仅保留为历史结果，不参与本次门禁。

### 3.2 Gate C：论文

两份 PDF 只使用同一组修复后的 accepted results、执行记录和图表。

| 评阅者 | P17 | P42 | 选择 |
| --- | ---: | ---: | --- |
| 统计建模 | 48 | 86 | P42 |
| 论文逻辑 | 55 | 75 | P42 |
| 竞赛评委 | 55 | 88 | P42 |
| 平均 | 52.6667 | 83.0000 | 差值 30.3333 |

3/3 选择 P42，无 P0，数学正确性未下降；因此 `gate_c_pass=true`。匿名 P17 为 1 页，P42
为 2 页；逐页检查通过，无缺字、裁切、重叠或不可见图表。旧的单评阅 `81 vs 59` 不参与门禁。

Gate C 只证明工作台版本在论证、验证口径和结论边界上更好。评阅者仍指出系数与区间、负二项
对照、重采样稳定性和完整多问覆盖不足，因此产物仍是子问题技术报告，不是完整竞赛论文。

## 4. 评分证据完整性

已逐文件计算 SHA-256，并与两份评分卡中的冻结值核对：

- 路线输入 `ROUTES.md`、统一量表 `RUBRIC.md`：一致；
- 匿名 PDF `paper-P17.pdf`、`paper-P42.pdf`：一致；
- 三份独立评阅报告：一致；
- R2 报告：一致。

当前汇总工件哈希为：

- `route_scorecard.json`：`6b7a159121d0f30bf4243634dbd62d19d743f3edf5a8398e8fb7a4049c9747be`；
- `paper_scorecard.json`：`1b32f6b46970e48a280224b9ae356d078a715623a7e3e5257e6e35ee916d7e66`；
- `RESULT.md`：`30358bbaf6a80d3077e0608f7eddf37955420f81206d6302288a204936c027af`。

## 5. 仓内知识与双轴检索

只读材料清点现登记 21 份 PDF，来源以 `root_name` 标识，不写入本机绝对路径。论文索引从 6 卡
增至 9 卡，新增：

- `fan-2021-thin-film-neural-networks.md`：薄膜光学前向模型与参数反演；
- `spirk-2021-biopolymer-thin-film-thickness.md`：薄膜厚度测量、可辨识性与外部验证；
- `cumcm-2017-a-ct-calibration-reconstruction.md`：已知模板标定、前向模型和逆向重建。

检索由单一重合分数改为结构与领域双轴证据，并保留兼容字段：

- `structural_similarity`：数据结构、任务链、约束和验证问题的相似性；
- `domain_similarity`：领域术语和对象的相似性；
- `overall_confidence`：只有结构和领域证据同时达到阈值才为 `high`。

回归结果中，葡萄酒卡对母婴题为结构 0.5000、领域 0.0000、`medium`，不再误报高置信；
`Thin-film neural networks for optical inverse problem` 对 CUMCM 2025 B 题为结构 0.6500、
领域 0.5000、`high`。薄膜综述为 `medium`，CT 和传热卡为 `low`。系统没有人工抬高分数。

## 6. 完整多问旧题验证

已创建独立运行 `runs/pilot-full-cumcm-2025-b-20260720`。权威问题清单冻结 CUMCM 2025 B 题
全部 3 个必做问题，依赖为 `Q1 -> Q2 -> Q3`。路线竞争包含三条实质不同的候选：

- `R1_PHYSICS_JOINT`：双角度共享厚度的物理光学全谱联合反演，推荐；
- `R2_PHASE_SIGNAL`：波数域相位解调与谐波诊断，备用；
- `R3_BAYES_HIERARCHICAL`：层次贝叶斯物理推断，因竞赛成本过高不推荐为主线。

推荐路线用 Snell 定律、Fresnel 系数、相位传播与 Airy/传递矩阵统一 Q1-Q3，并预注册
峰间距基线、多初值、参数剖面、连续块 Bootstrap、波段留出、合成恢复和厚度偏移验证。对于
缺失的相干长度、光斑重叠或仪器分辨率，只允许结论为 `UNKNOWN`，不得臆测。

当前状态：

- `status=WAITING_HUMAN_ROUTE`；
- `revision=1`；
- `route_locked=false`；
- `python scripts/codex/validate_state.py runs/pilot-full-cumcm-2025-b-20260720` 通过，无警告；
- `route_approval_request.json` 已绑定配置锁、路线候选和问题清单哈希。

按项目硬门禁，当前没有 `ROUTE_LOCK.json`，没有正式实验、厚度结果或论文。必须收到明确的人类
路线批准后，才能物化批准回执并继续。

## 7. 测试与静态检查

- 25 个测试文件分三组执行：32、51、76 项通过，合计 `159 passed`；
- 单进程全量 pytest 在 10 分钟超时前没有失败输出，分组执行均以退出码 0 完成；
- 知识与检索聚焦测试：`6 passed`；
- `python -m ruff check src scripts tests`：通过；
- `git diff --check`：通过，仅有 Windows CRLF 警告；
- 两个修复执行记录复验通过；完整旧题状态校验通过。

## 8. 已知状态例外

旧试点运行 `runs/desktop-e2e-20260719-training` 的实验和 R2 证据仍可复验，但该运行不满足当前
完整工作流状态合同。`validate_state.py` 明确失败，原因是：

1. 它绑定的 `profiles/generic.json` 内容哈希已随仓库演进改变；
2. 该旧运行创建于问题清单硬门之前，缺少 `problem/PROBLEM_MANIFEST.json`。

本轮没有静默重建其配置锁或问题清单，因为那会改写已绑定事实并使旧回执失去意义。该例外必须
保留在交付记录中；它不否定两项新 accepted result 的封条和独立复现，但禁止把此旧运行描述为
当前版本下完整状态合规的工作流。

## 9. 范围与下一确认点

本轮没有提交或推送 Git，也没有恢复任务开始前已有的删除项。后续应先由人类审核完整旧题的
三条路线与关键解释。若批准推荐方案，应明确回复批准 `R1_PHYSICS_JOINT` 为主路线、
`R2_PHASE_SIGNAL` 为备用，并接受双角度共享厚度、参考色散加有界修正及三层多光束判据。

在收到该回复前，工作流必须继续停在 `WAITING_HUMAN_ROUTE`。
