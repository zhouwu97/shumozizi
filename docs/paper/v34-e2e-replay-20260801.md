# v3.4 旧正式运行回放记录

使用 `python scripts/paper/replay_v34_closure.py` 在隔离副本上回放了三个旧正式结果运行。回放只复制并重新生成论文资产，不改写原运行；没有为缺失的结果、图或冷读回执补造文件。

| 旧运行 | 当前图文件 | 视觉机会 | 知识检索 | 视觉知识建议 | 长篇前置 | 冷读前置 |
|---|---:|---:|---|---:|---|---|
| `cumcm-2025-a-v32-20260728-019fa95e` | 6 | 13 | `no_relevant_match` | 0 | 阻断 | 阻断 |
| `cumcm-2025-b-sic-20260729` | 30 | 11 | `no_relevant_match` | 0 | 阻断 | 阻断 |
| `2025c` | 23 | 17 | `unavailable_with_reason`（缺少 `TASK_FINGERPRINT.json`） | 0 | 阻断 | 阻断 |

回放确认了以下闭环行为：

- 自动素材池会把旧运行的正式结果、建模合同、主张、边界和 `figures/current/` 的真实输出接入素材；三份回放的素材池分别为 62、32、70 条，且局部图绑定保留到具体输出文件，不使用整张 `figures/index.json` 摘要。
- 每份回放都生成了当前视觉机会池，并复核素材池摘要、故事板摘要和视觉政策；视觉机会的新鲜度为 current。
- 知识库检查确实执行了。两份有任务指纹的旧运行得到 `no_relevant_match`，第三份因缺少任务指纹得到带原因的 unavailable；因此没有把任何论文卡模式伪装成当前题方法，也没有自动生成图。
- 三份旧运行均因直接答案/机制素材不足、故事板占位字段和冷读记录缺失而未进入长篇或候选编译。这是硬门的预期阻断，不是回放失败后用报告文字覆盖缺口。

后续新生产运行必须先完成结构化知识检索、逐问直接答案和机制素材，填写实质故事板，再由当前真实图候选经过视觉批评与冷读后编译。回放脚本保留为后续每次协议变更的回归入口。
