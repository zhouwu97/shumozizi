# 2023 优秀论文第三轮离线学习审计

## 学习范围与完成标记

用户指定目录共清点到 14 篇 PDF。已有学习标记覆盖 10 篇，因此本轮未重复学习，尝试处理剩余的 `A165`、`A175`、`C050`、`C228` 四篇。`A165`、`A175`、`C050` 的文本层和页面可读，完成三张结构卡；`C228` 的中文字体映射损坏，文本提取为乱码，渲染后中文也缺失，无法可靠重建题面、公式、图表或页码证据，记录为 `unavailable_with_reason`。

本轮实际新增学习 3 篇，不足用户期望的 10 篇是因为指定目录中只有 4 篇未学习候选，且其中 1 篇不可读；不以乱码内容补足数量。累计状态见 `cumcm-2023-excellent-13-learning-markers.json`，本轮来源清单见 `cumcm-2023-excellent-round3-source-inventory.json`。

## 来源核对

| 源文件 | PDF 页数 | 论文 SHA-256 | 文本/渲染状态 | 学习状态 | 题面来源 |
|---|---:|---|---|---|---|
| `A165.pdf` | 45 | `63e129d60639f2a84beb094818290ab1035ddc0aa1d4b94c8189689d0523b401` | 可读 | learned | 论文第 2-3 页重建，未独立核对官方题面 |
| `A175.pdf` | 39 | `904914537e950e7faabbad018ebd1ecb410d5ff94d19731a79e2f3b971a28ac1` | 可读 | learned | 论文第 1-2 页重建，未独立核对官方题面 |
| `C050.pdf` | 44 | `8842b1e70acb03dc82171ea016b19aaabd0da043ce16878b384bc3c212adb510` | 可读 | learned | 论文第 2-3 页重建，未独立核对官方题面 |
| `C228.pdf` | 70 | `fb3f16b5294a6ea14781d3a824756af39a2700a2333d0df56c31ee3351188b10` | 中文字体映射损坏，渲染中文缺失 | unavailable_with_reason | 不建立卡 |

页数由 `pypdf` 读取，哈希由当前源文件重新计算；原始 PDF 保持只读，未复制进仓库。合集身份来自用户指定目录，没有发现可独立核验的奖项证明或许可证，因此学习卡只记录结构与批判性边界，不判断奖项等级。

## 本轮新增可迁移结构

1. `A165` 强调由光学机理拆分事件并共享评分器，再以空间规律降低布局搜索维度；可迁移的是评分器闭合、结构假设显式化和高精度复算要求，不是镜场参数或单峰结论。
2. `A175` 把大地、镜面和光锥坐标分成清晰接口，并用蜂窝布局构造可行解；可迁移的是坐标接口、可行布局族和双保真搜索，问题三继承前问布局的做法需要作为启发式分解审查。
3. `C050` 展示了从品类/单品数据画像到预测、利润决策和经营因素分析的递进；可迁移的是时间键、层级对象和预测到决策接口，不是同期价格—销量相关关系、灰色关联或模拟退火结果。

## 重点反例边界

- `A165`：有限近邻遮挡判断、网格化截断和既有布局继承都可能把局部改良误写成全局最优。
- `A175`：蜂窝结构提高可行性和速度，但可能排除自由布局；光锥/镜面采样和随机抽样需要独立收敛证据。
- `C050`：成本加成价格与销量的同期相关不是价格弹性或因果证据；预测曲线、启发式最优和灰色关联都不能替代时间留出、策略基线和不确定性验证。
- `C228`：在中文字体映射损坏且渲染内容缺失时，不能根据零散英文方法名猜测题目、方法链或结论。

## 单向生产边界

三张卡是离线能力资产，不读取或修改任何 `runs/` 目录，不触发生产论文流程。索引只允许后续 analysis 按数据结构、任务类型和验证风险检索结构模式；原论文数字、结论、代码、原始公式段和题目特定参数不得迁移到当前赛题，也不能作为 citation、claim evidence、结果来源或奖项判断。若后续补充可读的 `C228` 原文件，应重新核对哈希、页数和来源后再单独学习，不自动覆盖本轮不可用记录。

## 绘图方法补学

本轮在三张新增论文卡的“绘图方法与技巧”章节之外，另生成跨论文技法卡：`cumcm-2023-excellent-figure-techniques.md`。补学内容覆盖图形角色、数据结构到图形的选择、空间/轨迹/时间序列/预测/分布/优化/排程/多尺度绘图方法、版式与黑白可读性检查、图注的“观察—机制—决策后果—边界”闭环，以及三维饼图、双纵轴、只画收敛曲线、训练拟合冒充留出验证、只画路径不画覆盖等负面模式。该技法卡仍只提供表达和核查模板，不改变任何当前题目的模型、endpoint、实验结果或论文结论。
