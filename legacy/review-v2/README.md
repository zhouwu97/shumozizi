# legacy-review-v2（冻结）

本目录保留 Capability-First v3 之前的审核生命周期资产，包括按阶段审核 Skill、路线确认 Skill 与相关历史文档。它们仅用于复现既有运行、对照 A/B 和 Git 历史追踪。

新 v3 运行不得从本目录导入运行时逻辑，也不得重新启用其回执、裁决、闭环、按问审核或独立审核任务编排。等至少两套完整旧题完成 v3 验证且用户确认不再需要兼容后，才考虑进一步归档或删除旧实现。
