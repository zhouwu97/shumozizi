\section{问题二的模型建立与求解}

% ── 同 5_problem1 的结构规范 ──

\subsection{问题分析与模型选择}

% 说明为什么用 LASSO 而非岭回归或 Elastic Net？

采用 LASSO 回归筛选关键特征：
\[
  \min_{\theta} \frac{1}{2N} \sum_{i=1}^{N} (y_i - \theta^T x_i)^2 + \lambda \|\theta\|_1
\]

选择 LASSO 而非[替代方法]，因为[选择理由：如稀疏性假设成立/高维特征场景]。

\subsection{关键数学关系}

基于筛选的关键特征建立优化预测模型，引入交叉项捕捉交互效应。

\subsection{求解结果}

优化后测试集 $R^2$ 从 0.896 提升至 0.923，RMSE 降低约 15\%。

\subsection{结果解释与机制}

% 为什么 R² 提升？提升来自特征选择本身，还是交叉项，还是两者共同？

[指标]提升的主要驱动是[机制]，而非仅参数数量增加；具体而言，[解释]。

\subsection{适用边界}

本问结论在[条件]下成立；当[假设变化]时，[结论如何变化]。

\subsection{直接答案}
