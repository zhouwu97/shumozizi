% 附录 D：模型关键公式
% ---------------------------------------------------------------------------
% 写作阶段填写。把正文用到的主模型、目标函数、约束、显著性检验等关键公式
% 集中列出并编号，与正文交叉引用一致，方便评阅人核验推导。
% ---------------------------------------------------------------------------
%
% 建议结构（示例，按本题实际情况调整）：
%
% 主模型（问题一）：
% \begin{equation}
%   \logit Y_{ij} = \beta_0 + \beta_1\,\mathrm{weeks}_{ij} + \beta_2\,\mathrm{BMI}_j + u_j + \varepsilon_{ij}
% \end{equation}
%
% 达标比例与最佳时点（问题二）：
% \begin{equation}
%   P_G(t)=\mathbb{P}_G\!\big(\logit Y(t)\ge \logit 0.04\big),\qquad
%   t^*(G)=\min\{t:P_G(t)\ge \eta\}
% \end{equation}
%
% 每个公式编号与正文中的式号保持一致（正文引用 \texttt{\textbackslash label}/\texttt{\textbackslash ref}）。
