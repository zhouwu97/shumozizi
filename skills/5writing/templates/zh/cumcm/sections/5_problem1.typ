= 问题一的模型建立与求解

// ── 普通问题结构：问题与选择 → 关键数学关系 → 求解结果 → 直接答案
// ── 若本题为核心问题，额外增加：结果解释与机制、适用边界
// ── 删除本注释后填写实际内容

== 问题分析与模型选择

// 说明为什么选择这个模型？有哪些候选，为何排除其他？
// 不要只写"采用 X 方法"，要写"采用 X 而非 Y，因为……"

采用 Z-score 标准化：$x' = frac(x - mu, sigma)$。

本问采用[方法名]，而非[候选替代]，原因在于[选择理由]。

== 关键数学关系

// 写核心公式和推导，而非逐步罗列所有计算。

多元线性回归：

$ y = theta_0 + theta_1 x_1 + theta_2 x_2 + dots.c + theta_n x_n + epsilon $

== 求解结果

随机森林测试集 $R^2$ 达到 0.896，优于线性回归的 0.834。

== 结果解释与机制

// 必填：结果为什么呈现当前形态？哪个因素是主要驱动？
// 禁止只写"可见模型效果良好"。

[数值]表明[含义]，主要原因是[机制]。
移除[某特征/约束]后，[指标]下降[幅度]，证明该因素的贡献是实质性的。

== 适用边界

// 必填：哪些假设改变后结论会变化？

本问结论在[条件]下成立；若[关键假设变化]，则[结论将如何变化]。

== 直接答案

// 清晰回答题面要求的问题。
