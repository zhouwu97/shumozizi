= 模型评价与推广

// 本节不要写通用套话（"理论基础扎实""多模型对比确保最优性"）。
// 针对本题的实际结果和建模选择，具体讨论局限和推广边界。

== 本题特定局限

// 必填：针对本题的实际局限，而非通用缺点列表。
// 例：本模型假设[X]，而实际中[Y]，导致[Z]情形下结论可能不准确。
// 例：优化只保证局部最优；全局最优性在[条件]下有[证据/无证据]支持。

+ [针对本题的局限 1，说明其影响范围和程度]；
+ [针对本题的局限 2]；
+ [哪些假设在实际场景中可能不成立]。

== 活跃约束与改善路径

// 移除或放松哪个约束/假设，对结果影响最大？
// 讨论"瓶颈约束"而非"方法的理论美感"。

当前方案的主要限制来自[活跃约束/假设]；若[改善该约束]，可预期[效果]。

== 推广条件

// 不要写无前提的泛化（"可推广至金融风控、工业生产……"）。
// 说明推广所需的前提条件：什么情况下本文方法仍然适用？

本文方法在满足以下条件时可推广：[条件 1]；[条件 2]。
当[条件不满足]时，需要[替代方案或修改]。
