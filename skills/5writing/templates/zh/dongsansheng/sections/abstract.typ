#align(center)[#text(size: 14pt, weight: "bold")[[论文题目]]]
#v(0.65em)
#align(center)[#text(size: 14pt, weight: "bold")[摘 #h(1em) 要]]

// 摘要结构（完成后删除本注释）：
// 背景与核心困难 → 关键建模定义 → 主方法 →
// 最重要 2-3 个结果（含数字） → 规律与决策含义 → 可信边界
//
// 禁止按"问题一/二/三"逐句点名，禁止用"建立了完整的数学模型体系"开头。
[背景：核心困难是什么，使这道题非显然]。本文将该困难定义为[建模结构]，
以[主方法]求解。得到[最重要数值结果，含具体数字]。
分析发现[核心规律]，据此[决策含义]。
模型在[参数范围]内鲁棒；当[关键假设]改变时结论可能随之调整。

#v(0.6em)
#block[
  #set par(first-line-indent: 0pt)
  #text(font: ("Heiti SC", "STHeiti", "Songti SC"), weight: "bold")[关键词：] [关键词 1]；[关键词 2]；[关键词 3]；[关键词 4]
]
